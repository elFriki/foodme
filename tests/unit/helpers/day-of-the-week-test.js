
import { dayOfTheWeek } from 'foodme/helpers/day-of-the-week';
import { module, test } from 'qunit';

module('Unit | Helper | day of the week');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = dayOfTheWeek([42]);
  assert.ok(result);
});

