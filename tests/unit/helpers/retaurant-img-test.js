
import { retaurantImg } from 'foodme/helpers/retaurant-img';
import { module, test } from 'qunit';

module('Unit | Helper | retaurant img');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = retaurantImg([42]);
  assert.ok(result);
});

